name: Publish Spin Artifact
on:
  push:
    branches:
      - main
    tags:
      - "**"
    paths-ignore:
      - "README.md"
      - "LICENSE"
jobs:
  publish:
    name: Build and Publish Spin Artifact
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine tag
        id: tag
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            RAW="${GITHUB_REF#refs/tags/}"
            echo "value=${RAW#v}" >> "$GITHUB_OUTPUT"
          else
            echo "value=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Rust with wasm32-wasip2
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2

      - name: Install Node.js 26
        uses: actions/setup-node@v4
        with:
          node-version: "26"

      - name: Install Spin
        uses: fermyon/actions/spin/setup@v1

      - name: Build Spin App
        run: spin build

      - name: Login to GHCR
        run: |
          spin registry login ghcr.io \
            --username "${{ github.actor }}" \
            --password "${{ secrets.GITHUB_TOKEN }}"

      - name: Push OCI Artifact
        run: |
          spin registry push \
            "ghcr.io/${{ github.repository }}:${{ steps.tag.outputs.value }}"
          spin registry push \
            "ghcr.io/${{ github.repository }}:latest"
