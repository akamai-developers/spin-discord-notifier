spin_manifest_version = 2

[application]
name = "discord-notifier"
version = "0.1.0"
authors = ["Thorsten Hans <thans@akamai.com>"]
description = ""

[variables]
discord_webhook_url = { required = true, secret = true, description = "The Discord webhook URL to send notifications to." }

[[trigger.http]]
route = "/..."
component = "frontend"

[[trigger.http]]
route = "/api/..."
component = "api"

[component.frontend]
source = { url = "https://github.com/spinframework/spin-fileserver/releases/download/v0.3.0/spin_static_fs.wasm", digest = "sha256:ef88708817e107bf49985c7cefe4dd1f199bf26f6727819183d5c996baa3d148" }
files = [{ source = "frontend/dist", destination = "/" }]

[component.frontend.build]
workdir = "frontend"
command = ["npm install", "npm run build"]

[component.api]
source = "api/target/wasm32-wasip2/release/api.wasm"
allowed_outbound_hosts = [ "https://discord.com/"]

[component.api.variables]
discord_webhook_url = "{{ discord_webhook_url }}"

[component.api.build]
command = "cargo build --target wasm32-wasip2 --release"
workdir = "api"
watch = ["src/**/*.rs", "Cargo.toml"]
